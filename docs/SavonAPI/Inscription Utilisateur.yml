info:
  name: Inscription Utilisateur
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:8080/api-savon/register
  body:
    type: json
    data: |-
      {
        "id":null,
        "nom":"Moulin",
        "prenom":"Timothée",
        "email":"admin@email.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

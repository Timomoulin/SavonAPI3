info:
  name: Ajout commentaire
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:8080/api-savon/v1/commentaire
  body:
    type: json
    data: |-
      {
        "id":null,
        "texte":"Super recette !!",
      "idRecette":1
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Count Recette
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/api-savon/v1/recette/count

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
